# Core functionality
requests>=2.31.0
pandas>=2.2.0
numpy>=1.26.0

# Schema validation and JSON parsing
jsonschema>=4.19.0
json5>=0.9.12           # supports JSON with comments
commentjson>=0.9.0       # alternative for .jsonc files

# Database and file handling
sqlite3                  # built-in with Python, no need to pip install
